<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />

    <application>
        <service
            android:name="com.openstore.app.installer.ApkInstallerService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name="com.openstore.app.installer.InstallationReceiver"
            android:exported="false"
            >
            <intent-filter>
                <action android:name="com.openstore.app.installer.INSTALL_COMMIT"/>
                <action android:name="com.openstore.app.installer.UNINSTALL_ACTION"/>
            </intent-filter>
        </receiver>
    </application>
</manifest>
